---
import { getCollection } from "astro:content";

const allPosts = await getCollection("reviews");
const allTags = allPosts.map((tag) => tag.data.categories).flat();

console.log(allTags);
---

<span> </span>
